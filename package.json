{
  "name": "tgparser",
  "version": "0.1.0",
  "description": "TDLib-based channel refresher that stores aggregated JSON snapshots in SQLite.",
  "main": "src/refresh-monthly.js",
  "scripts": {
    "refresh": "node src/refresh-monthly.js",
    "visualize": "node scripts/visualize.js"
  },
  "dependencies": {
    "@google/genai": "latest",
    "tdl": "latest"
  }
}
